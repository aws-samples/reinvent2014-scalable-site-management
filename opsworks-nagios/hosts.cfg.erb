# Host definitions
#
# Autogenerated by OpsWorks.

<% @hosts.each do |name, host| -%>
define host {
  use common-host
  address <%= name %>
  host_name <%= name %>
  hostgroups all,<%= host['hostgroups'].keys.sort.join(',') %>
}
<% end -%>
